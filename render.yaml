services:
  - type: web
    name: gaspro-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /
    plan: free
    autoDeploy: true
